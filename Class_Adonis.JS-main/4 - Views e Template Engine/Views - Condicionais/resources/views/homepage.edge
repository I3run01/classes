<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Meu primeiro site com AdonisJS</title>
</head>
<body>
    <h1>Hello World</h1>
    <h2> Usuários: </h2>

    <ul>
        @each( (usuario) in usuarios)
            @if(usuario.admin)
                <li> {{usuario.nome}} - Administrador </li>
            @else
                <li> {{usuario.nome}} - Usuario </li>
            @end
        @end
    </ul>

    <h2> Método 2 Usuários: </h2>
    <ul>
        @each( (usuario) in usuarios)
                <li> {{usuario.nome}} - {{usuario.admin ? 'Administrador' : 'Usuário'}} </li>
        @end
    </ul>
    

</body> 
</html>